/**
 * @jsx React.DOM
 */
'use strict';

var React = require('react');

var UserPage = React.createClass({

  render: function() {
    return (
      <div />
    );
  }

});

module.exports = React.createFactory(UserPage);